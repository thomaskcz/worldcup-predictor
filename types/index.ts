export type NavItem = {
  href: string;
  label: string;
};

export type {
  KnockoutWinnerPick,
  LeaderboardRow,
  Match,
  MatchStage,
  Prediction,
  UserScore,
} from "@/types/database";

export type { CompetitionRulesJson, ScoreBreakdown } from "@/types/rules";
